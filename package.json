{
  "name": "dayacid-bot",
  "version": "3.0.0",
  "description": "DayaCID Bot — Telegram anti-spam bot for trading communities",
  "type": "module",
  "scripts": {
    "deploy": "vercel --prod",
    "test": "node --test"
  },
  "engines": {
    "node": "24.x"
  }
}